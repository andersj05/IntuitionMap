# Synthetic private-workflow fixture

Every record in this directory is project-authored and fictional. It exists
only to test consent, immutable import, redaction, annotation, protected splits,
export, and deletion. It contains no user or third-party personal data.
